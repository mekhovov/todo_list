.sidebar
  .page-header
    %br
    %br
    -if @tasks.empty?
      %h3 No tasks yet
      %h2 
        Add project:
        = link_to 'New Project', new_project_path, :class => 'btn primary'
      %br
  = render 'tasks/assingned'
  = render 'tasks/todo'
