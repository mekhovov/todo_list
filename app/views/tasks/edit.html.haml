%h1 Editing task

= render 'form'

= link_to 'Show', @task, :class => 'btn info'

= link_to 'Delete', @task, :confirm => 'Are you sure?', :method => :delete, :class => 'btn danger'

= link_to 'Back', :back, :class => 'btn'
